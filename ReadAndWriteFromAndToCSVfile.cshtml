//https://code-maze.com/csharp-basics-streamwriter-streamreader/           




List<string> listA = new List<string>();
            listA.Clear();
            
            //get from csv file to list array
            using (var reader = new StreamReader(@"C:\\Users\\test.csv"))
            {
                while (!reader.EndOfStream)
                {
                    var line = reader.ReadLine();
                    var values = line.Split(',');

                    listA.Add(values[0]);
                  
                }
            }
            //Write to csv file from list
                using (var writer = new StreamWriter(@"CW:\\+ location"))
            {
                foreach (DataRow eachrow in LocalDataTable.Rows)

                {
                    string phone1 = eachrow["TEL1"].ToString();
                    string phone2 = eachrow["TEL2"].ToString();
                    string id = eachrow["ID"].ToString();
                    var line = string.Format("{0},{1},{2}", phone1, phone2, id);
                    writer.WriteLine(line);


                }
                writer.Close();
            }


            listA.Clear();
